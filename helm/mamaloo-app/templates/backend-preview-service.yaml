{{- if and .Values.backend.rollout.enabled (eq .Values.backend.rollout.strategy "blueGreen") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mamaloo-app.fullname" . }}-backend-preview
  labels:
    {{- include "mamaloo-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
    service-type: preview
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.backend.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "mamaloo-app.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
{{- end }}
